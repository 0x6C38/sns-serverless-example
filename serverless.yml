
service: sns-example

provider:
  name: aws
  runtime: nodejs8.10

functions:
  hello:
    handler: handler.hello

resources:
  Resources:
    SQSQueue:
      Type: AWS::SNS::Topic
      Properties:
        TopicName: "Test-Topic"
